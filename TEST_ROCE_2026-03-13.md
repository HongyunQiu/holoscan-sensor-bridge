# Holoscan Sensor Bridge RoCE 测试记录（2026-03-13）

## 测试目标

- 在 DGX Spark 环境下，使用 HSB Emulator + 两个 CX-7 100G 光口，验证 `vb1940_player.py` 及相关 Holoviz GUI 能否在 RoCE 模式下运行。
- 如可行，给出配置步骤，便于后续接入真实 Hololink 硬件前做仿真验证。

## 环境信息

- **主机**：DGX Spark（Ubuntu 24.04.2 LTS）
- **GPU**：NVIDIA GB10
- **网卡**：双 CX-7 100G，接口 `enp1s0f0np0`（host）与 `enp1s0f1np1`（emulator）通过 100G 光模块互联
- **软件版本**：
  - hololink-demo:2.5.0（容器）
  - holoscan-sensor-bridge 仓库 HEAD
  - Python 3.12.3 + pytest 9.0.2（容器内另外安装）

## 操作步骤

1. **LinuxReceiver 基线**
  - 在宿主直接运行 `build/hsb-emulator/examples/serve_linux_single_vb1940_frames 192.168.0.2`，并在 holodemo 容器里执行 `linux_vb1940_player.py --hololink 192.168.0.2 --camera-mode 0`。
  - 结果：Holoviz 正常显示测试图像，证明 emulator 在 Linux socket 模式下工作正常。
2. **手动 RoCE 模式尝试**
  - 将 emulator 端口 `enp1s0f1np1` 使用 `scripts/nsisolate.sh` 放入独立 network namespace，并用 `scripts/nsexec.sh` 在该 namespace 内启动 emulator。
  - 在容器里运行 `vb1940_player.py --hololink 192.168.0.2 --ibv-name rocep1s0f1 --ibv-port 1`。
  - 结果：程序在 `camera.configure()` 卡死，`SYSTEM_FSM_STATE` 恒为 `0x4`，Holoviz “No response”。
3. **pytests 官方路径**
  - 在容器里执行 `python3 -m pytest tests/test_vb1940.py::test_vb1940_player --vb1940 --hsb --headless --frame-limit 60 --hw-loopback enp1s0f1np1 enp1s0f0np0`，让脚本自动隔离接口、启动 emulator。
  - 结果：`Enumerator.find_channel(192.168.0.2)` 直接抛 `RuntimeError: Device with 192.168.0.2 not found`。
4. **回退验证**
  - 将接口从 namespace 中撤回，恢复 LinuxReceiver 流程，再次确认 GUI 能正常打开，以排除硬件/光纤问题。

## 结论

- 目前版本的 HSB Emulator **无法**配合 RoceReceiverOp 建立 RoCE 控制/数据面，表现为：
  - `Enumerator.find_channel` 找不到 192.168.0.2；
  - 即使找到，也无法使 VB1940 状态机跳出 `SYSTEM_FSM_STATE = 0x4`。
- Linux sockets / COE 模式一切正常，可用于 GUI 演示及算法开发。
- 真正的 RoCE 验证需要等待实体 Hololink 硬件或者官方发布支持 RoCE 的 emulator。

## 建议与后续

1. 等硬件到位后，直接按官方指南接入 Hololink + VB1940，再运行 `vb1940_player.py`，无需依赖 emulator。
2. 若仍希望在 emulator 上调试 RoCE，可关注后续 release notes，或向 NVIDIA 团队反馈 “HSB emulator RoCE 支持” 需求。
3. 当前 GUI 调测建议保持 LinuxReceiver 路线；需要重现步骤仅需：
  - `./build/hsb-emulator/examples/serve_linux_single_vb1940_frames 192.168.0.2`
  - `python3 examples/linux_vb1940_player.py --hololink 192.168.0.2 --camera-mode 0`

